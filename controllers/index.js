const home = require('./home-controller');
const user = require('./user-controller');
const project = require('./project-controler');
const team = require('./team-controler');

module.exports = {
    home,
    user,
    project,
    team
};